[user]
	name = ggila
	email = ggilaber@student.42.fr
[core]
	editor = vim
[push]
	default = matching
[diff]
	tool = vimdiff
[alias]
	s = status
	ss = status --porcelain
	a = add
	au = add -u
	cm = commit
	ch = checkout
	dl = difftool
	slog = log --oneline --graph --decorate --pretty=format:'%C(auto)%h %<(8,trunc)%C(cyan)%an%Creset%C(auto)%d %s %C(black bold)%ar'
[color]
	ui = auto
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
[color "status"]
	added = yellow
	changed = green
	untracked = cyan
[merge]
	tool = vimdiff
	conflictstyle = diff3
	prompt = false
