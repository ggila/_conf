[user]
	name = ggila
	email = gautier.gilabert@artefact.com
[core]
	editor = vim
[push]
	default = matching
[diff]
	tool = vimdiff
[alias]
	s = status
	ss = status --porcelain
	a = add
	cm = commit
	ch = checkout
	br = bramch
	slog = log --oneline --graph --decorate --all --pretty=format:'%C(auto)%h %<(8,trunc)%C(cyan)%an%Creset%C(auto)%d %s %C(black bold)%ar'
[color]
	ui = auto
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
[color "status"]
	added = yellow
	changed = green
	untracked = cyan
[merge]
	tool = vimdiff
	conflictstyle = diff3
	prompt = false
